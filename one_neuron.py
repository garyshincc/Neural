import numpy as np
import random
class Neuron():
	def __init__:
		weight = random.randint(-1, 1)
		bias = random.randint(a, b)

	def propagate(input):
		linear = np.dot(input, self.W1)
		# node
		non_linear = self.sigmoid(self.z2)
		return non_linear

myNeuron = Neuron()

print myNeuron.propagate(10)


